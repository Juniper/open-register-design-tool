#include "output.h"
//#include <stdio.h>
int main() {
    unsigned long int localMaster = MASTER;
    unsigned long int localBit = BIT(2);

    unsigned long int localMaster1 = MASTER1;
    unsigned long int localGenmask = GENMASK(1, 0);

   return MASTER + MASTER1;
}
